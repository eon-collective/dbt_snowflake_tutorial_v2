packages:
  # installing the package below raises a dbt deps failed with dbt runtime exception: 
  # Found duplicate project "dbt_utils". This occurs when a dependency has the same project name as some
  # other dependency.
  # - package: gitlabhq/snowflake_spend
  #   version: 1.2.0
  # - git: "https://github.com/dbt-labs/dbt-utils.git" # git URL
  # revision: 1.0.0 # tag or branch name
  - package: dbt-labs/codegen
    version: 0.9.0
  - package: dbt-labs/audit_helper
    version: 0.7.0
  - package: tnightengale/dbt_meta_testing
    version: 0.3.6
    # This package logs information about source, seed, & model nodes to the command line
    # in pipe-delimited csv format.
  - git: "https://github.com/eon-collective/dbt_readme_helper.git"
    revision: 0.1.1

    # The package builds a mart of tables and views describing the project that it is installed in.
  - git: "https://github.com/eon-collective/dbt_artifacts_eon.git"
    revision: 0.2.0